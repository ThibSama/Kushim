# kushim-app

Vite + React + TypeScript private application.

Routes:

- `/dashboard`
- `/actifs`
- `/actifs/:id`
- `/transactions`
- `/parametres`

Local port: `5173`

Run:

```bash
copy .env.example .env
npm install
npm run dev
```

Checks:

```bash
npm run lint
npm run build
npm audit
```
